import { FunctionComponent } from 'react';
import { ReactLiveTimeProps, Localization } from './interfaces';
declare function ReactLiveTime(props: ReactLiveTimeProps): FunctionComponent;
declare namespace ReactLiveTime {
    let localization: Localization;
    let prefix: string;
    let suffix: string;
}
export declare function setLocale(locale: Object): void;
export default ReactLiveTime;
